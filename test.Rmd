---
title: "Test de las funciones de utils"
author: "Unax"
---

```{r}
source("utils.R")

```

```{r}
library(ggplot2)
library(grid) 
data("mpg", package = "ggplot2")
df <- mpg

```

```{r}

cty_width <- equal_width_discretize(df[["cty"]], n_bins = 4)

cty_freq <- equal_frequency_discretize(df[["cty"]], n_bins = 4)

```

```{r}
cat("Conteos por bin (equal-frequency):\n")
print(table(cty_width, useNA = "ifany"))
cat("Conteos por bin (equal-width):\n")
print(table(cty_freq, useNA = "ifany"))
```

```{r}
print(variance(df[['displ']]))

print(entropy(as.character(df[['class']])))
```

